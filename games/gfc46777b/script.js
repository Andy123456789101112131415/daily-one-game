function init_gfc46777b(container) {
function(container){var colors=['#7c3aed','#06b6d4','#10b981','#f59e0b','#ef4444'];var score=0;var targetColor='';var grid=[];var scoreEl,messageEl,gridEl,targetEl;function randomColor(){return colors[Math.floor(Math.random()*colors.length)];}function newRound(){targetColor=randomColor();targetEl.style.backgroundColor=targetColor;grid=[];var correctIndex=Math.floor(Math.random()*8);for(var i=0;i<8;i++){if(i===correctIndex){grid.push(targetColor);}else{var c;do{c=randomColor();}while(c===targetColor);grid.push(c);}}renderGrid();messageEl.textContent='';}function renderGrid(){gridEl.innerHTML='';for(var i=0;i<8;i++){var cell=document.createElement('div');cell.className='cm-cell';cell.style.backgroundColor=grid[i];cell.dataset.index=i;cell.addEventListener('click',function(e){handleClick(parseInt(e.currentTarget.dataset.index));});gridEl.appendChild(cell);}}function handleClick(index){var selected=grid[index];if(selected===targetColor){score++;scoreEl.textContent='得分: '+score;messageEl.textContent='正确!';messageEl.style.color='#10b981';gridEl.children[index].classList.add('cm-anim-correct');setTimeout(newRound,300);}else{messageEl.textContent='错了!';messageEl.style.color='#ef4444';gridEl.children[index].classList.add('cm-anim-wrong');}}var wrap=document.createElement('div');wrap.className='cm-wrap';wrap.innerHTML='<div class="cm-header"><span class="cm-title">色彩匹配</span><span class="cm-score">得分: 0</span></div><div class="cm-target"><span class="cm-target-label">找到颜色:</span><div class="cm-target-color"></div></div><div class="cm-grid"></div><div class="cm-message"></div><button class="cm-btn">重来</button>';container.innerHTML='';container.appendChild(wrap);scoreEl=wrap.querySelector('.cm-score');messageEl=wrap.querySelector('.cm-message');gridEl=wrap.querySelector('.cm-grid');targetEl=wrap.querySelector('.cm-target-color');var btn=wrap.querySelector('.cm-btn');btn.addEventListener('click',function(){score=0;scoreEl.textContent='得分: 0';newRound();});newRound();}
}